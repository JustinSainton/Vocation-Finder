<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Your Vocational Profile</title>
    <style>
        @page {
            margin: 36px 42px 42px;
        }

        body {
            font-family: DejaVu Serif, Georgia, serif;
            color: #1c1917;
            font-size: 12px;
            line-height: 1.55;
        }

        h1, h2, h3 {
            font-weight: normal;
            margin: 0;
        }

        h1 {
            font-size: 24px;
            margin-bottom: 6px;
        }

        h2 {
            font-size: 13px;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            color: #57534e;
            margin-top: 24px;
            margin-bottom: 10px;
            padding-bottom: 6px;
            border-bottom: 1px solid #d6d3d1;
        }

        p {
            margin: 0 0 10px;
        }

        .subtitle {
            color: #78716c;
            font-style: italic;
            margin-bottom: 20px;
        }

        .meta-grid {
            width: 100%;
            border-collapse: collapse;
            margin: 14px 0 18px;
        }

        .meta-grid td {
            width: 33.33%;
            vertical-align: top;
            padding: 10px 12px;
            border: 1px solid #e7e5e4;
            background: #f8f7f2;
        }

        .meta-label {
            display: block;
            font-size: 10px;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            color: #78716c;
            margin-bottom: 4px;
        }

        .list-card {
            margin-bottom: 8px;
            padding: 10px 12px;
            border-left: 3px solid #a8a29e;
            background: #f5f5f0;
        }

        .step-row {
            margin-bottom: 8px;
        }

        .step-number {
            display: inline-block;
            width: 18px;
            color: #57534e;
            font-weight: bold;
        }

        .footer {
            margin-top: 28px;
            padding-top: 14px;
            border-top: 1px solid #d6d3d1;
            color: #78716c;
            font-size: 10px;
        }
    </style>
</head>
<body>
    <h1>Your Vocational Profile</h1>
    <p class="subtitle">A synthesis of your gifts, inclinations, and next vocational steps</p>

    @if($profile->opening_synthesis)
        <h2>Opening Synthesis</h2>
        {!! nl2br(e($profile->opening_synthesis)) !!}
    @endif

    @if($profile->vocational_orientation)
        <h2>Vocational Orientation</h2>
        {!! nl2br(e($profile->vocational_orientation)) !!}
    @endif

    <table class="meta-grid">
        <tr>
            <td>
                <span class="meta-label">Primary Domain</span>
                {{ $profile->primary_domain ?: 'Not specified' }}
            </td>
            <td>
                <span class="meta-label">Mode of Work</span>
                {{ $profile->mode_of_work ?: 'Not specified' }}
            </td>
            <td>
                <span class="meta-label">Secondary Orientation</span>
                {{ $profile->secondary_orientation ?: 'Not specified' }}
            </td>
        </tr>
    </table>

    @if(!empty($profile->primary_pathways))
        <h2>Primary Pathways</h2>
        @foreach($profile->primary_pathways as $pathway)
            <div class="list-card">{{ $pathway }}</div>
        @endforeach
    @endif

    @if($profile->specific_considerations)
        <h2>Specific Considerations</h2>
        {!! nl2br(e($profile->specific_considerations)) !!}
    @endif

    @if(!empty($profile->next_steps))
        <h2>Next Steps</h2>
        @foreach($profile->next_steps as $index => $step)
            <div class="step-row">
                <span class="step-number">{{ $index + 1 }}.</span>
                <span>{{ $step }}</span>
            </div>
        @endforeach
    @endif

    @if($profile->ministry_integration)
        <h2>Ministry Integration</h2>
        {!! nl2br(e($profile->ministry_integration)) !!}
    @endif

    <div class="footer">
        <p>
            This vocational profile was generated by AI and is intended as a tool for reflection and discernment,
            not as professional career counseling.
        </p>
        <p>Return to your results anytime at {{ $resultsUrl }}</p>
    </div>
</body>
</html>
